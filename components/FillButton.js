import React from 'react'
import styles from './FillButton.module.scss';

export default function FillButton() {
  return (
    <button type="button" className={styles.fill}>Download CV</button>
  )
}
